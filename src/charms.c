#include "global.h"
#include "items.h"
/*
void AddCharm(u16 itemId)
{
    for (int i = 0; i < MAX_ACTIVE_CHARMS; i++)
    {
        if (gActiveCharms[i] == 0) // Find the first empty slot
        {
            gActiveCharms[i] = itemId;
            return;
        }
    }
}

void RemoveCharm(u16 itemId)
{
    for (int i = 0; i < MAX_ACTIVE_CHARMS; i++)
    {
        if (gActiveCharms[i] == itemId)
        {
            gActiveCharms[i] = 0; // Remove the charm
            return;
        }
    }
}

bool IsCharmActive(u16 itemId)
{
    for (int i = 0; i < MAX_ACTIVE_CHARMS; i++)
    {
        if (gActiveCharms[i] == itemId)
            return true;
    }
    return false;
}

void ClearAllCharms(void)
{
    for (int i = 0; i < MAX_ACTIVE_CHARMS; i++)
    {
        gActiveCharms[i] = 0;
    }
}
*/